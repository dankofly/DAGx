---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class:list={['rounded-2xl border border-slate-700/40 bg-slate-900 p-6 md:p-8 transition-colors hover:border-slate-600/60', className]}>
  <slot />
</div>
